## 🔧 Updated `.github/workflows/blog-posts.yml`
name: Update Blog Posts

on:
  schedule:
    - cron: '0 * * * *'      # Runs every hour
  workflow_dispatch:        # Manual trigger

jobs:
  update-blog-posts:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Update Medium Blog Posts
        uses: gautamkrishnar/blog-post-workflow@master
        with:
          # Use your actual Medium RSS feed URL here:
          feed_list: "https://medium.com/feed/@ritavkashyap123"

      - name: Commit and Push Changes
        run: |
          git config --local user.email "ritavkashyap123@gmail.com"
          git config --local user.name "Ritav Kashyap"
          git add README.md
          git commit -m "Updated blog posts"
          git push
